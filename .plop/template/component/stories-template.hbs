import type { Meta, StoryObj } from '@storybook/react'

import {{pascalCase component_name}} from './{{pascalCase component_name}}'

const meta: Meta<typeof {{pascalCase component_name}}> = {
  title: 'components/{{pascalCase component_name}}',
  component: {{pascalCase component_name}},
  tags: ['autodocs'],
  args: {
    name: 'name',
  },
}

export default meta

type Story = StoryObj<typeof {{pascalCase component_name}}>

export const Default: Story = {}
